# This package is managed with git-buildpackage # and not yet pristine-tar.

[DEFAULT]
# use pristine-tar:
#pristine-tar = True

[git-dch]
# Include 7 digits of the commit ID in the changelog entry:
id-length = 7
